// créations de variables
let idEvent;
let currentParticipantsNumber;
let maxParticipantsNumber;
let idUser;

function getValueById(id){

}

/**
 * vérifie si le nombre de participants maximum est atteint
 * @param {int} current 
 * @param {int} max 
 * @returns bool
 */
function isClosedEvent(current, max){
  return(currentParticipantsNumber==maxParticipantsNumber);
}

function notifyUser(message){
  
}